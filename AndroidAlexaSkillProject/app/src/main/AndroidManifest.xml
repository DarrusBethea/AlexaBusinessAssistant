<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidalexaskillproject">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"

        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreenReturnFragment">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".MainListActivity">
        </activity>
        <activity android:name=".ProfitReturnAddFragment">
        </activity>

        <activity android:name=".ProfitReturnEditFragment">
        </activity>

        <activity android:name=".PieChartReturnFragment">
        </activity>

        <activity android:name=".BarChartReturnFragment">
        </activity>

        <activity android:name=".ProfitsAddFragments">
        </activity>

        <activity android:name=".MainListGrossProfitActivity">
        </activity>

        <activity android:name=".LineChartReturnFragment">
        </activity>

        <activity android:name=".LoginScreenReturnFragment">
        </activity>

    </application>

</manifest>