import { SpeechBuilder } from 'jovo-core';
import { AuthorizationResponse } from '../modules/ProactiveEvent';
export interface ApiCallOptions {
    endpoint: string;
    method?: string;
    path: string;
    permissionToken?: string;
    json?: any;
}
export declare class AlexaAPI {
    /**
     *
     * @param {ApiCallOptions} options
     * @returns {Promise<any>}
     */
    static apiCall(options: ApiCallOptions): Promise<unknown>;
    static progressiveResponse(speech: string | SpeechBuilder, requestId: string, apiEndPoint: string, apiAccessToken: string): Promise<unknown>;
    /**
     * TODO
     * Proactive Events Authorization API call
     */
    static proactiveEventAuthorization(clientId: string, clientSecret: string): Promise<AuthorizationResponse>;
}
