import { BaseApp, Db, PluginConfig } from 'jovo-core';
interface Config extends PluginConfig {
    path?: string;
}
export declare class FileDb2 implements Db {
    /**
     * Creates paths recursively
     * @param {string} targetDir
     * @param {boolean} isRelativeToScript
     */
    private static mkDirByPathSync;
    needsWriteFileAccess: boolean;
    config: Config;
    constructor(config?: Config);
    install(app: BaseApp): void;
    errorHandling(): void;
    /**
     * Returns object for given primaryKey
     * @param {string} primaryKey
     * @return {Promise<any>}
     */
    load(primaryKey: string): Promise<any>;
    save(primaryKey: string, key: string, data: any, updatedAt?: string): Promise<any>;
    delete(primaryKey: string): Promise<any>;
    private readFile;
    private saveFile;
    private deleteFile;
}
export {};
